# Captured personal activity data — never commit
activity.db
*.db
*.db-journal
*.sqlite
*.sqlite3

# Local config — may contain the user's own API key
config.json

# Captured screenshots (personal) — never commit
shots/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# Build artifacts (distribute the exe via GitHub Releases, not git)
build/
dist/
*.spec
packaging/icon.ico
packaging/icon-512.png
packaging/icon.icns
packaging/icon.iconset/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
